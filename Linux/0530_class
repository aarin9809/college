서버가 본인것이라는걸 증명하는 방법 :
1. 이메일(제한있음) 
2. DNS 정보 수정(DNS관리자가 해줘야함)
3. http 안에 파일 갖다놓기
	documentRoot에 파일을 만들고 등록되어있으면 이메일주고(서버접근가능, 웹서버세팅, 소스코드 접속 가능 -> 서버 주인이다 인증)
4. https

멀티로 도메인을 만들수 있어서 
virtual host를 써라

libmaxminddb
해당 지역정보를 가지고있는 lib

customlog 끝에 common이 있다면 common을 택하면 되

goaccess -c access_log-20230326

goaccess access_log-20230326 -o report.html --log-format=COMMON 을 해서 나온 레포트를
documentRoot 경로에 레포트 파일을 넣어야함

vhost파일에 COMMON인지 COMBINED인지 확인 꼭 하기.

logwatch
